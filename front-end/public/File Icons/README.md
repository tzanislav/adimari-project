# NAS file icons

Add one PNG per file extension, named in lowercase (for example, `fbx.png` or `pdf.png`).

The NAS explorer uses `folder.png` for folders and `file.png` for files without an extension.
