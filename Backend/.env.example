AWS_ACCESS_KEY_ID=<set>
AWS_SECRET_ACCESS_KEY=<set>
AWS_REGION=eu-west-1
S3_BUCKET_NAME="mesharch"
AWS_USE_PATH_STYLE_ENDPOINT=false
NODE_ENV=development
DEV_MODE=development
EMAILJS_PRIVATE_KEY= <set>
OPENAI_API_KEY=<set>
PORT=5001
# The backend must stay behind Nginx; never set this to a LAN/public address.
BACKEND_BIND_HOST=127.0.0.1
CLICKUP_API_KEY=<set>
CLICKUP_TEAM_ID=37458550
MONGODB_URI=<set>
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Private File Server - a dedicated IAM identity and private S3 bucket.
# Do not reuse AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, or S3_BUCKET_NAME above;
# those variables belong to the existing uploader/Rekognition integration.
# Store real values only in the backend deployment's secret environment, never in Vite.
FILE_SERVER_AWS_ACCESS_KEY_ID=<set>
FILE_SERVER_AWS_SECRET_ACCESS_KEY=<set>
FILE_SERVER_AWS_REGION=eu-west-1
FILE_SERVER_BUCKET_NAME=<set>
FILE_SERVER_S3_PREFIX=files/
# Optional, share-only File Sync namespace in the same bucket. This never
# expands file-manager browse, upload, move, or delete permissions.
# FILE_SYNC_S3_PREFIX=files-sync/
# Folder-share ZIPs are built in this separate private prefix. The File Server
# IAM identity needs GetObject/PutObject/DeleteObject plus multipart
# AbortMultipartUpload/ListMultipartUploadParts permission here as well as on
# FILE_SERVER_S3_PREFIX. Do not put this prefix beneath files/.
FILE_SERVER_SHARE_ARCHIVE_PREFIX=file-share-archives/
# A folder-share worker uses one low-memory, 16 MiB-part ZIP build at a time.
# Raise these only after confirming worker capacity. Do not apply a blanket S3
# expiration rule to this prefix while permanent share links are active; a
# lifecycle rule may still abort incomplete multipart uploads.
FILE_SERVER_SHARE_ARCHIVE_MAX_FILES=5000
FILE_SERVER_SHARE_ARCHIVE_MAX_BYTES=21474836480
FILE_SERVER_SHARE_ARCHIVE_BUILD_CONCURRENCY=1
FILE_SERVER_PUBLIC_BASE_URL=http://localhost:5173

# Uploads use browser-to-S3 multipart URLs, so Express never receives file bytes.
# 50,000,000,000,000 bytes is S3's current 50 TB / 48.8 TiB multipart limit.
FILE_SERVER_MAX_UPLOAD_BYTES=50000000000000
FILE_SERVER_MULTIPART_PART_SIZE_BYTES=67108864
FILE_SERVER_UPLOAD_PART_URL_TTL_SECONDS=900
FILE_SERVER_DOWNLOAD_URL_TTL_SECONDS=300
